import React from "react";

import "../styles/Main.modules.css";

const ComparisionBoxContainer = ({ children }) => {
  return <div className="comparison-ops">{children}</div>;
};

export default ComparisionBoxContainer;
